/**
 * @picasso/canvas-hypothesis
 *
 * PRD 3.5's hypothesis tracker: claims that carry their own falsifiers, resolve
 * themselves when the data arrives, and accumulate into a calibration record.
 *
 * "This is the feature that turns the canvas into an accountability instrument
 * rather than a mood board."
 */

export * from './hypothesis.js';
export * from './calibration.js';
export * from './node.js';
