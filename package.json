{
  "name": "ray-research-tools",
  "private": true,
  "version": "0.0.0",
  "description": "Alphalytica / The Grand Chessboard research tooling",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
