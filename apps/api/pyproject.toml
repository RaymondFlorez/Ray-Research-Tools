# Placeholder project metadata for the GeoGlobe backend.
# Real dependencies (fastapi, uvicorn, sqlalchemy, geoalchemy2, psycopg, pgvector,
# anthropic, ...) are added in BUILD_PROMPTS.md Step 5.

[project]
name = "geoglobe-api"
version = "0.0.0"
description = "GeoGlobe backend: data service + LLM agent orchestration (placeholder)."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
